[project]
name = "yt-rag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "apaks", email = "ap.akshay008@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=7.3.0",
    "langchain>=1.3.14",
    "python-dotenv>=1.2.2",
]

[project.scripts]
yt-rag = "yt_rag:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
